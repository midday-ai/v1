{
  "name": "@v1/stripe",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "clean": "rm -rf .turbo node_modules",
    "lint": "biome check .",
    "format": "biome format --write .",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@v1/logger": "workspace:*",
    "@v1/supabase": "workspace:*",
    "stripe": "^12.0.0",
    "next": "^14.0.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "typescript": "^5.0.0",
    "bun-types": "latest"
  }
}
